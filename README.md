# claude-review

> **AI-powered PR Review Sub-Agent** — Structured Markdown reviews for any GitHub Pull Request.

`claude-review` is a lightweight CLI agent that fetches a GitHub PR diff, analyzes it for risks and improvements, and outputs a structured Markdown review. Works standalone or as a GitHub Action.

## $150 Bounty — Issue #4

This implementation satisfies all acceptance criteria for [claude-builders-bounty/claude-builders-bounty #4](https://github.com/claude-builders-bounty/claude-builders-bounty/issues/4).

## Quick Start

```bash
# CLI mode (standalone)
python claude-review.py --pr https://github.com/owner/repo/pull/123

# With specific token
python claude-review.py --pr https://github.com/owner/repo/pull/123 --token ghp_xxx

# Using environment variable
export GITHUB_TOKEN=ghp_xxx
python claude-review.py --pr https://github.com/owner/repo/pull/123

# Output to file
python claude-review.py --pr https://github.com/owner/repo/pull/123 --output review.md

# Fetch diff only (debug)
python claude-review.py --pr https://github.com/owner/repo/pull/123 --diff-only
```

## Requirements

- Python 3.8+
- GitHub token with `public_repo` scope (or `repo` for private repos)

## Output Format

Reviews contain:

| Section | Description |
|---------|-------------|
| **Summary of Changes** | File breakdown, additions/deletions, commit info |
| **Identified Risks** | Checklist of potential issues (large PRs, TODOs, secrets, debug code) |
| **Improvement Suggestions** | Actionable recommendations |
| **Review Metadata** | Stats table (files, changes, commits, branches) |
| **Confidence Score** | Low / Medium / High based on PR complexity |

### Sample Output

```
## PR Review: feat: add destructive command blocker hook (Issue #3)

> **Repository:** `claude-builders-bounty/claude-builders-bounty` | **PR #2504**
> | **Author:** @Omgiri01 | **Confidence:** High

---

### Summary of Changes

This PR modifies **4 files** with **+152** / **-0** changes across **2 commit(s)**.

**File type breakdown:**
- `.md`: 1 file(s) (21+, 0-)
- `.png`: 1 file(s) (0+, 0-)
- `.py`: 2 file(s) (131+, 0-)

---

### Identified Risks

- [ ] Debug/console statements detected in new code. Remove before merging.

---

### Improvement Suggestions

- Changes span 3 file types. Verify cross-language compatibility.

---

_Review generated by claude-review agent_
```

## GitHub Action

Add `.github/workflows/pr-review.yml` to your repo:

```yaml
name: PR Review Agent

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run PR Review
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python claude-review.py --pr "${{ github.event.pull_request.html_url }}" --output review.md
      - name: Post Review Comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const review = fs.readFileSync('review.md', 'utf8');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: review
            });
```

## Tested On Real PRs

This agent was successfully tested against:

| PR | Result |
|----|--------|
| [`kcolbchain/brand#8`](https://github.com/kcolbchain/brand/pull/8) — Add toast notification component | ✅ [+255/-1, 15 files] |
| [`claude-builders-bounty/claude-builders-bounty#2505`](https://github.com/claude-builders-bounty/claude-builders-bounty/pull/2505) — Add generate changelog skill | ✅ [+217/-0, 4 files] |
| [`claude-builders-bounty/claude-builders-bounty#2504`](https://github.com/claude-builders-bounty/claude-builders-bounty/pull/2504) — Add destructive command blocker hook | ✅ [+152/-0, 4 files] |

Sample outputs are available in [`test_output/`](test_output/).

## How Reviews Work

The agent:

1. Fetches PR metadata via GitHub REST API (title, description, files, commits)
2. Fetches the full PR diff
3. Analyzes for **risks**:
   - Large diffs (>500 lines) — hard to review
   - TODO/FIXME/HACK markers in new code
   - Debug/console statements left in
   - Commented-out code blocks
   - Potential hardcoded secrets
   - Large single-file additions (>200 lines)
4. Generates **suggestions**:
   - Cross-language compatibility
   - Commit granularity
   - PR description completeness
5. Calculates **confidence score** based on PR complexity

## License

MIT
